From 4a1565d7c0f6172308c23f2c0bcdb5dae556b9a6 Mon Sep 17 00:00:00 2001
From: Bernd Geiser <bg@ferncast.de>
Date: Fri, 22 Sep 2023 16:11:05 +0200
Subject: [PATCH 3/6] nlecomposition-disable-dot-output

---
 plugins/nle/nlecomposition.c | 14 +++++++-------
 1 file changed, 7 insertions(+), 7 deletions(-)

diff --git a/plugins/nle/nlecomposition.c b/plugins/nle/nlecomposition.c
index c1f7577..c0bb1c6 100644
--- a/plugins/nle/nlecomposition.c
+++ b/plugins/nle/nlecomposition.c
@@ -2523,18 +2523,18 @@ _is_ready_to_restart_task (NleComposition * comp, GstEvent * event)
 
 
   if (comp->priv->seqnum_to_restart_task == seqnum) {
-    gchar *name = g_strdup_printf ("%s-new-stack__%" GST_TIME_FORMAT "--%"
-        GST_TIME_FORMAT "", GST_OBJECT_NAME (comp),
-        GST_TIME_ARGS (comp->priv->current_stack_start),
-        GST_TIME_ARGS (comp->priv->current_stack_stop));
+    /* gchar *name = g_strdup_printf ("%s-new-stack__%" GST_TIME_FORMAT "--%" */
+    /*     GST_TIME_FORMAT "", GST_OBJECT_NAME (comp), */
+    /*     GST_TIME_ARGS (comp->priv->current_stack_start), */
+    /*     GST_TIME_ARGS (comp->priv->current_stack_stop)); */
 
     GST_INFO_OBJECT (comp, "Got %s with proper seqnum"
         " done with stack reconfiguration %" GST_PTR_FORMAT,
         GST_EVENT_TYPE_NAME (event), event);
 
-    GST_DEBUG_BIN_TO_DOT_FILE_WITH_TS (GST_BIN (comp),
-        GST_DEBUG_GRAPH_SHOW_ALL, name);
-    g_free (name);
+    /* GST_DEBUG_BIN_TO_DOT_FILE_WITH_TS (GST_BIN (comp), */
+    /*     GST_DEBUG_GRAPH_SHOW_ALL, name); */
+    /* g_free (name); */
 
     if (GST_EVENT_TYPE (event) == GST_EVENT_EOS) {
       GST_INFO_OBJECT (comp, "update_pipeline DONE");
-- 
2.39.2

